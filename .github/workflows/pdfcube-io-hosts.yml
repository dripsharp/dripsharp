name: PdfCube.IO required macOS hosts

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  package-proof:
    runs-on: [self-hosted, linux, x64, dripsharp-proof]
    timeout-minutes: 60
    env:
      DRIPSHARP_WORKERS: 22
      JAVA_TOOL_OPTIONS: -Xmx28g
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "25"

      - uses: DeLaGuardo/setup-clojure@13.4
        with:
          cli: latest

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Verify proof-runner resources
        shell: bash
        run: |
          available_kib=$(awk '/MemAvailable/ { print $2 }' /proc/meminfo)
          available_cpu=$(getconf _NPROCESSORS_ONLN)
          if (( available_kib < 33554432 || available_cpu < 22 )); then
            echo "PdfCube.IO package proof requires at least 32 GiB available RAM and 22 CPUs."
            exit 1
          fi

      - name: Pack, consume, and differentially verify
        run: clojure -J-Xmx28g -M:run differential pdfcube pdfcube-io

      - name: Upload complete local feed
        uses: actions/upload-artifact@v4
        with:
          name: pdfcube-io-local-feed
          path: target/package-proof/feed/*.nupkg
          if-no-files-found: error

      - name: Upload canonical proof
        uses: actions/upload-artifact@v4
        with:
          name: pdfcube-io-canonical-proof
          path: validation-output/pdfcube-io-differential
          if-no-files-found: error

  host-smoke:
    needs: package-proof
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos
            architecture: x64
            runner: macos-15-intel
          - os: macos
            architecture: arm64
            runner: macos-15
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - uses: actions/download-artifact@v4
        with:
          name: pdfcube-io-local-feed
          path: target/pdfcube-io-host-feed

      - name: Build and smoke the package-only consumer
        shell: pwsh
        run: |
          $project = "validation/pdfcube-io/PdfCube.IO.HostSmoke.csproj"
          $config = "validation/pdfcube-io/NuGet.Config"
          $packages = "target/pdfcube-io-host-packages"
          $trace = "target/host-${{ matrix.os }}-${{ matrix.architecture }}.tsv"
          $canonical = "targets/pdfcube/validation/io-canonical.tsv"
          dotnet --info | Out-File `
            "target/dotnet-${{ matrix.os }}-${{ matrix.architecture }}.txt" `
            -Encoding utf8
          dotnet restore $project --configfile $config --packages $packages `
            --no-cache --force --force-evaluate
          dotnet build $project --nologo --verbosity:minimal --no-restore `
            --no-incremental -warnaserror
          dotnet run --project $project --no-build --no-restore -- `
            $trace $canonical "${{ matrix.os }}" "${{ matrix.architecture }}"

      - name: Upload host evidence
        uses: actions/upload-artifact@v4
        with:
          name: pdfcube-io-${{ matrix.os }}-${{ matrix.architecture }}
          path: |
            target/host-${{ matrix.os }}-${{ matrix.architecture }}.tsv
            target/dotnet-${{ matrix.os }}-${{ matrix.architecture }}.txt
          if-no-files-found: error
