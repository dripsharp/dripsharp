name: PdfCube.PdfBox printing required macOS hosts

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  package-proof:
    runs-on: [self-hosted, linux, x64]
    timeout-minutes: 60
    env:
      DRIPSHARP_WORKERS: 22
      JAVA_TOOL_OPTIONS: -Xmx28g
    steps:
      - uses: actions/checkout@v4

      - name: Check out the pinned PDFBox source
        run: git submodule update --init --depth 1 research/pdfbox

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "25"

      - uses: DeLaGuardo/setup-clojure@13.4
        with:
          cli: latest

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Verify proof-runner resources
        shell: bash
        run: |
          available_kib=$(awk '/MemAvailable/ { print $2 }' /proc/meminfo)
          available_cpu=$(getconf _NPROCESSORS_ONLN)
          if (( available_kib < 33554432 || available_cpu < 22 )); then
            echo "PdfCube.PdfBox printing proof requires at least 32 GiB available RAM and 22 CPUs."
            exit 1
          fi

      - name: Pack, consume, and differentially verify
        run: clojure -J-Xmx28g -M:run proof pdfcube

      - name: Upload complete local feed
        uses: actions/upload-artifact@v4
        with:
          name: pdfcube-pdfbox-printing-local-feed
          path: target/package-proof/feed/*.nupkg
          if-no-files-found: error

      - name: Upload canonical printing proof
        uses: actions/upload-artifact@v4
        with:
          name: pdfcube-pdfbox-printing-canonical-proof
          path: validation-output/pdfcube-pdfbox-printing-differential
          if-no-files-found: error

  host-smoke:
    needs: package-proof
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos
            architecture: x64
            runner: macos-15-intel
          - os: macos
            architecture: arm64
            runner: macos-15
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - uses: actions/download-artifact@v4
        with:
          name: pdfcube-pdfbox-printing-local-feed
          path: target/pdfcube-pdfbox-printing-host-feed

      - uses: actions/download-artifact@v4
        with:
          name: pdfcube-pdfbox-printing-canonical-proof
          path: target/pdfcube-pdfbox-printing-canonical-proof

      - name: Build and smoke the package-only printing consumer
        shell: pwsh
        run: |
          $project = "validation/pdfcube-pdfbox-printing/PdfCube.PdfBox.PrintingHostSmoke.csproj"
          $config = "validation/pdfcube-pdfbox-printing/NuGet.Config"
          $packages = "target/pdfcube-pdfbox-printing-host-packages"
          $canonical = "target/pdfcube-pdfbox-printing-canonical-proof/upstream-java.tsv"
          $trace = "target/pdfbox-printing-host-${{ matrix.os }}-${{ matrix.architecture }}.tsv"
          dotnet --info | Out-File `
            "target/pdfbox-printing-dotnet-${{ matrix.os }}-${{ matrix.architecture }}.txt" `
            -Encoding utf8
          dotnet restore $project --configfile $config --packages $packages `
            --no-cache --force --force-evaluate
          dotnet build $project --nologo --verbosity:minimal --no-restore `
            --no-incremental -warnaserror
          dotnet run --project $project --no-build --no-restore -- `
            $trace $canonical "${{ matrix.os }}" "${{ matrix.architecture }}"

      - name: Upload host evidence
        uses: actions/upload-artifact@v4
        with:
          name: pdfcube-pdfbox-printing-${{ matrix.os }}-${{ matrix.architecture }}
          path: |
            target/pdfbox-printing-host-${{ matrix.os }}-${{ matrix.architecture }}.tsv
            target/pdfbox-printing-dotnet-${{ matrix.os }}-${{ matrix.architecture }}.txt
          if-no-files-found: error
