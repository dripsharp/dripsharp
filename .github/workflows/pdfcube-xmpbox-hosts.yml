name: PdfCube.XmpBox supported hosts

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  package-proof:
    runs-on: [self-hosted, linux, x64, vibeformer-proof]
    timeout-minutes: 60
    env:
      VIBEFORMER_WORKERS: 22
      JAVA_TOOL_OPTIONS: -Xmx28g
    steps:
      - uses: actions/checkout@v4

      - name: Check out the pinned PDFBox source
        run: git submodule update --init --depth 1 research/pdfbox

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "25"

      - uses: DeLaGuardo/setup-clojure@13.4
        with:
          cli: latest

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Verify proof-runner resources
        shell: bash
        run: |
          available_kib=$(awk '/MemTotal/ { print $2 }' /proc/meminfo)
          available_cpu=$(getconf _NPROCESSORS_ONLN)
          if (( available_kib < 33554432 || available_cpu < 22 )); then
            echo "PdfCube.XmpBox package proof requires at least 32 GiB RAM and 22 CPUs."
            exit 1
          fi

      - name: Pack, consume, and differentially verify
        working-directory: vibeformer
        run: clojure -J-Xmx28g -M:run pdfcube-xmpbox-metadata-differential

      - name: Upload complete local feed
        uses: actions/upload-artifact@v4
        with:
          name: pdfcube-xmpbox-local-feed
          path: vibeformer/target/package-proof/feed/*.nupkg
          if-no-files-found: error

      - name: Upload canonical proof
        uses: actions/upload-artifact@v4
        with:
          name: pdfcube-xmpbox-canonical-proof
          path: vibeformer/validation-output/pdfcube-xmpbox-metadata-differential
          if-no-files-found: error

      - name: Stage synchronized XmpBox fixtures
        shell: bash
        run: |
          fixture_root="vibeformer/target/pdfcube-xmpbox-host-fixtures"
          mkdir -p "$fixture_root"
          cp -R research/pdfbox/xmpbox/src/test/resources/. "$fixture_root/"

      - name: Upload synchronized XmpBox fixtures
        uses: actions/upload-artifact@v4
        with:
          name: pdfcube-xmpbox-host-fixtures
          path: vibeformer/target/pdfcube-xmpbox-host-fixtures
          if-no-files-found: error

  host-smoke:
    needs: package-proof
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows
            architecture: x64
            runner: windows-2025
          - os: windows
            architecture: arm64
            runner: windows-11-arm
          - os: linux
            architecture: x64
            runner: ubuntu-24.04
          - os: linux
            architecture: arm64
            runner: ubuntu-24.04-arm
          - os: macos
            architecture: x64
            runner: macos-15-intel
          - os: macos
            architecture: arm64
            runner: macos-15
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - uses: actions/download-artifact@v4
        with:
          name: pdfcube-xmpbox-local-feed
          path: vibeformer/target/pdfcube-xmpbox-host-feed

      - uses: actions/download-artifact@v4
        with:
          name: pdfcube-xmpbox-canonical-proof
          path: vibeformer/target/pdfcube-xmpbox-canonical-proof

      - uses: actions/download-artifact@v4
        with:
          name: pdfcube-xmpbox-host-fixtures
          path: vibeformer/target/pdfcube-xmpbox-host-fixtures

      - name: Build and smoke the package-only consumer
        shell: pwsh
        run: |
          $project = "vibeformer/validation/pdfcube-xmpbox/PdfCube.XmpBox.HostSmoke.csproj"
          $config = "vibeformer/validation/pdfcube-xmpbox/NuGet.Config"
          $packages = "vibeformer/target/pdfcube-xmpbox-host-packages"
          $resources = "vibeformer/target/pdfcube-xmpbox-host-fixtures"
          $canonical = "vibeformer/target/pdfcube-xmpbox-canonical-proof/upstream-java.tsv"
          $trace = "vibeformer/target/xmpbox-host-${{ matrix.os }}-${{ matrix.architecture }}.tsv"
          dotnet --info | Out-File `
            "vibeformer/target/xmpbox-dotnet-${{ matrix.os }}-${{ matrix.architecture }}.txt" `
            -Encoding utf8
          dotnet restore $project --configfile $config --packages $packages `
            --no-cache --force --force-evaluate
          dotnet build $project --nologo --verbosity:minimal --no-restore `
            --no-incremental -warnaserror
          dotnet run --project $project --no-build --no-restore -- `
            $trace $resources $canonical `
            "${{ matrix.os }}" "${{ matrix.architecture }}"

      - name: Upload host evidence
        uses: actions/upload-artifact@v4
        with:
          name: pdfcube-xmpbox-${{ matrix.os }}-${{ matrix.architecture }}
          path: |
            vibeformer/target/xmpbox-host-${{ matrix.os }}-${{ matrix.architecture }}.tsv
            vibeformer/target/xmpbox-dotnet-${{ matrix.os }}-${{ matrix.architecture }}.txt
          if-no-files-found: error
